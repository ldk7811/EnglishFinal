import { defineConfig } from 'wxt';

// See https://wxt.dev/api/config.html
export default defineConfig({
    manifest: {
        name: "Hypertext Attention",
        description: "Makes wikipedia viewable only by hyperlinks",
        version: "0.1.0",
        permissions: []
    }
});
